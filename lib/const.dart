import 'package:flutter/material.dart';

const KwelcomeMainText =TextStyle(
  fontSize: 60,
  fontWeight: FontWeight.bold,
  color: Colors.white,
);
const KdisText=TextStyle(fontSize: 20, color: Colors.white);
const loginButText=TextStyle(
  fontSize: 20,
  color: Colors.white,

);
const signupText=TextStyle(
  fontSize: 20,
  color: Colors.white,
);
const inputFieldDecoration =InputDecoration(
  hintText: "Enter Email",
  contentPadding:
  EdgeInsets.symmetric(vertical: 10, horizontal: 20),
  border: const OutlineInputBorder(
    borderRadius: const BorderRadius.all(
      const Radius.circular(10.0),
    ),
  ),
);


const inputFieldDecoration2 =InputDecoration(
  hintText: "Enter password",
  contentPadding:
  EdgeInsets.symmetric(vertical: 10, horizontal: 20),
  border: const OutlineInputBorder(
    borderRadius: const BorderRadius.all(
      const Radius.circular(10.0),
    ),
  ),
);

const inputDecuration3=InputDecoration(
  hintText: "Enter Email",
  contentPadding:
  EdgeInsets.symmetric(vertical: 10, horizontal: 20),
  border: const OutlineInputBorder(
    borderRadius: const BorderRadius.all(
      const Radius.circular(10.0),
    ),
  ),
);

const inputDecuration4=InputDecoration(
  hintText: "Enter Password",
  contentPadding:
  EdgeInsets.symmetric(vertical: 10, horizontal: 20),
  border: const OutlineInputBorder(
    borderRadius: const BorderRadius.all(
      const Radius.circular(10.0),
    ),
  ),
);